# CLAUDE.md - Mobile Development Template
# Generated by Claude-Flow (Official Template)
# https://github.com/ruvnet/claude-flow/wiki/CLAUDE-MD-Mobile-Development

## 🚨 CRITICAL: MOBILE DEVELOPMENT PARALLEL EXECUTION

MANDATORY RULE: For mobile development projects, ALL operations MUST be concurrent/parallel.

### 🔴 MOBILE-SPECIFIC CONCURRENT PATTERNS

- Cross-Platform Parallel: Develop iOS and Android simultaneously
- Component Development: Create multiple screens/components in parallel
- Native Module Integration: Build platform-specific modules concurrently
- Testing Automation: Generate unit, integration, and e2e tests in parallel
- Asset Management: Handle images, fonts, and resources concurrently

### ⚡ MOBILE DEVELOPMENT GOLDEN RULE: "CROSS-PLATFORM PARALLEL EXECUTION"

✅ CORRECT Mobile Development Pattern:
```
[BatchTool]:
  // iOS Components (Parallel)
  - Write("ios/Components/HomeScreen.swift", iosHomeScreen)
  - Write("ios/Components/ProfileScreen.swift", iosProfileScreen)
  - Write("ios/Components/SettingsScreen.swift", iosSettingsScreen)
  
  // Android Components (Parallel)
  - Write("android/app/src/main/java/com/app/HomeActivity.kt", androidHome)
  - Write("android/app/src/main/java/com/app/ProfileActivity.kt", androidProfile)
  - Write("android/app/src/main/java/com/app/SettingsActivity.kt", androidSettings)
  
  // React Native Screens (Parallel)
  - Write("src/screens/HomeScreen.tsx", rnHomeScreen)
  - Write("src/screens/ProfileScreen.tsx", rnProfileScreen)
  - Write("src/screens/SettingsScreen.tsx", rnSettingsScreen)
```

## 📱 PROJECT CONTEXT

### Project Type
- **Framework:** React Native + Expo
- **Platform:** iOS + Android
- **Build System:** EAS (Expo Application Services)
- **Language:** TypeScript

## 🐝 SWARM ORCHESTRATION

### Recommended Topology
```bash
claude-flow hive init --topology hierarchical --agents 4
```

### Agent Distribution:
- 1 Mobile Architect (coordinator)
- 1 React Native Developer (cross-platform)
- 1 Mobile Tester (comprehensive testing)
- 1 Mobile DevOps (deployment and CI/CD)

## 🔧 DEVELOPMENT PATTERNS

### React Native Structure
```
src/
├── screens/           // Screen components (parallel)
├── components/        // Reusable components (parallel)
├── navigation/        // Navigation setup
├── services/          // API and business logic (parallel)
├── hooks/             // Custom hooks (parallel)
├── utils/             // Utility functions
├── assets/            // Images, fonts, etc.
└── __tests__/         // Tests (parallel)
```

### Concurrent File Creation Pattern
```bash
[BatchTool]:
  // Create screen with navigation, styles, and tests
  - Write("src/screens/ProductScreen.tsx", screenComponent)
  - Write("src/screens/ProductScreen.styles.ts", screenStyles)
  - Write("__tests__/screens/ProductScreen.test.tsx", screenTests)
  - Write("src/navigation/ProductNavigator.tsx", navigation)
```

## 🧪 TESTING STRATEGY

### Testing Pyramid (Parallel Execution)
```bash
[BatchTool - Mobile Testing]:
  // Unit Tests (Parallel)
  - Bash("npm run test:unit")
  
  // Integration Tests (Parallel)
  - Bash("npm run test:integration")
  
  // E2E Tests (Parallel)
  - Bash("npx detox test --configuration ios.sim.debug")
  - Bash("npx detox test --configuration android.emu.debug")
```

## 🚀 DEPLOYMENT & CI/CD

### Build Process (Parallel Execution)
```bash
# Cross-platform build pipeline
ios_build:
  - "eas build --platform ios"
  - "eas submit --platform ios"

android_build:
  - "eas build --platform android"
  - "eas submit --platform android"

react_native_build:
  - "npx react-native bundle --platform ios --dev false"
  - "npx react-native bundle --platform android --dev false"

quality_checks:
  - "npm run lint"
  - "npm run type-check"
  - "npm run test:e2e"
```

### Environment Configuration
```bash
# Development
EXPO_PUBLIC_API_URL=https://dev-api.yourapp.com

# Staging
EXPO_PUBLIC_API_URL=https://staging-api.yourapp.com

# Production
EXPO_PUBLIC_API_URL=https://api.yourapp.com
```

## 📊 PERFORMANCE TARGETS

- **Cold Start:** < 5 seconds
- **Memory Usage:** < 100MB baseline
- **Bundle Size:** < 50MB initial download
- **Frame Rate:** 60fps for animations

## 🔐 SECURITY CHECKLIST

- Data encryption: TLS 1.3 for network, encrypted local storage
- Secure storage: iOS Keychain, Android Keystore
- Certificate pinning: SSL pinning for API calls
- Biometric auth: Touch ID, Face ID support

## 🎨 UI/UX PATTERNS

### Platform-Specific Styling
```bash
[BatchTool - Platform Styling]:
  // iOS-specific styles
  - Write("src/styles/ios.styles.ts", iosStyles)
  
  // Android-specific styles
  - Write("src/styles/android.styles.ts", androidStyles)
```

## 📋 SUCCESS CRITERIA

- [ ] App runs in Expo Go
- [ ] iOS build succeeds via EAS
- [ ] Android build succeeds via EAS
- [ ] All unit tests pass
- [ ] E2E tests pass
- [ ] Performance targets met
- [ ] App Store submission ready

## 📚 REFERENCE

- Official Template: https://github.com/ruvnet/claude-flow/wiki/CLAUDE-MD-Mobile-Development
- Installation: https://github.com/ruvnet/claude-flow/wiki/Installation-Guide
- Quick Start: https://github.com/ruvnet/claude-flow/wiki/Quick-Start
